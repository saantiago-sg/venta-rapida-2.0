import { Routes } from '@angular/router';

import { OnboardingPage } from './pages/onboarding-page/onboarding-page';

export const ONBOARDING_ROUTES: Routes = [{ path: '', component: OnboardingPage }];
